<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bottom_navigation_graph"
    app:startDestination="@id/listPostsFragment">

    <fragment
        android:id="@+id/listPostsFragment"
        android:name="pl.alex.personaldict.ListPosts"
        android:label="fragment_list_posts"
        tools:layout="@layout/fragment_list_posts" />
    <fragment
        android:id="@+id/calendarFragment"
        android:name="pl.alex.personaldict.Calendar"
        android:label="fragment_calendar"
        tools:layout="@layout/fragment_calendar" />
    <fragment
        android:id="@+id/favourites_fragment"
        android:name="pl.alex.personaldict.Favourites"
        android:label="fragment_favourites"
        tools:layout="@layout/fragment_favourites" />
</navigation>